use std::io;
fn main(){
    let v :vec<i64> = Vec::new();
    println!("\n  the length o vec::new is {}," v.len());

    let v = vec!["grace", "effiong", "basil" , "kareem" , "susan"];
     println!("\n the length of the vec macro is : {}", v.len());
}